# CaseAgent 实施计划（按迭代维护）

## 文档用途

这份文档是给 AI 和研发共同执行的实施计划。

维护原则：
- 计划、进度、下一步都写在“迭代详情”里，不再拆分到独立的“进度快照/优先待办”。
- 更新时优先改当前迭代的“已完成”和“下一步”。

## 项目终态

CaseAgent 围绕“需求文档 + 架构知识”形成可审核的测试用例闭环：

1. 导入需求文档与架构知识。
2. 文档清洗、分块、向量化、检索。
3. 识别受影响产品和模块。
4. 基于需求与知识上下文生成测试用例。
5. 审核、修改、提交测试用例。
6. 必要时沉淀知识库更新建议。

## 固定技术决策

- 前端：Vue 3 + Vite。
- 后端：Golang + Gin + Bun ORM + eino + eino-ext。
- 数据库：PostgreSQL + pgvector。
- 模型配置统一走配置文件，并支持环境变量覆盖。
- chat 当前支持 Ark；embedding 当前支持 Ark / OpenAI-compatible。
- embedding 维度通过 `model.embedding.dimensions` 显式配置。
- 当前工程对 `Qwen3-Embedding-8B` 使用 `2000` 维输出，以兼容 `pgvector + ivfflat` 索引约束。
- 如后续需要高于 `2000` 维的向量方案，需要先重评向量类型与索引策略。
- Google Drive 集成通过本地 `gws drive files export` 命令调用，无 Docker 依赖。

## 迭代总览（截至 2026-04-29）

| 迭代 | 主题 | 状态 |
| --- | --- | --- |
| 迭代 1 | 数据闭环（文档/知识库 -> 向量化 -> 检索） | 进行中 |
| 迭代 2 | 生成闭环（需求 -> 检索增强 -> Agent -> 用例落库） | 进行中（已启动基础能力） |
| 迭代 3 | 产品化闭环（前端工作台 + 审核体验 + 运维性） | 未开始 |

## 迭代详情

### 迭代 1：数据闭环（最高优先级）

**目标**

把“文档/知识库写入 -> 向量化 -> 检索”链路做成稳定可重复的基础能力。

**已完成（截至 2026-04-29）**

- 后端基础结构已落地：配置加载、Gin 路由、Bun 模型、迁移脚本、服务层骨架。
- pgvector 自定义向量编码/解码已完成，支持按当前配置自动校正向量维度。
- 文档处理链路已支持：异步处理、Google Drive 导入、base64 图片清洗、Markdown 分块与二次切分、`document_chunks` 写入、原文持久化、单文档重处理。
- 知识库链路已支持：CRUD、整篇文档 embedding、处理状态跟踪、单知识库重处理。
- 文档/知识库检索 API 已支持 `query` 与 `queries` 两种方式；multi-query 合并去重已完成。
- 向量健康检查与批量重建入口已存在，可治理历史异维度向量。
- `2026-04-28` 已验证后端可连接本地 PostgreSQL、自动应用 `backend/migrations/001_init.sql` 并成功监听启动。

**下一步**

- 补齐文档上传到检索命中的整链路联调记录（可重复执行）。
- 使用批量重建入口完成历史异维度向量治理并记录结果。
- 补一组稳定的检索回归样例（至少覆盖文档检索与知识库检索各 1 条）。

**验收标准**

- 文档链路可从上传稳定走到检索命中。
- 知识库链路可从创建稳定走到检索命中。
- 当前 `2000` 维配置下，不再出现历史异维度导致的检索错误或混乱结果。

### 迭代 2：生成闭环

**目标**

完成“需求输入 -> 受影响范围确认 -> 检索增强 -> Agent 生成 -> 用例落库”的主业务闭环。

**已完成（截至 2026-04-29）**

- 任务服务已具备：需求文档装载、受影响产品/模块初步识别、需求文档检索上下文拼装、任务审核/生成入口状态流转基础校验。
- 已接入 Agent 服务并支持生成测试用例落库。
- 生成结果已附带受影响产品/模块上下文字段，支持审核追踪。
- DeepAgent 与 4 个子 Agent 骨架已存在；子 Agent 结果已有基础汇总去重、失败隔离和 DeepAgent 二次收敛整理。

**下一步**

- 补齐 parent retriever 细化策略，提升片段到父文档的上下文一致性。
- 完善 DeepAgent 协调逻辑（任务拆分、汇总策略、失败恢复）。
- 提升生成质量控制（去重、结构化约束、可读性和可追溯性）。
- 完成一条可复现的“选定需求文档 -> 生成用例 -> 入库 -> 查询”联调样例。

**验收标准**

- 能基于选定需求文档稳定生成结构正确的测试用例。
- 生成结果可追溯到受影响产品/模块与检索上下文。
- 结果稳定落库，并可被审核接口读取与修改。

### 迭代 3：产品化闭环

**目标**

在后端主流程可用后，补齐前端工作台与审核体验，使系统不依赖手工 API 调用即可完整走通。

**已完成（截至 2026-04-29）**

- Vue 3 + Vite 前端脚手架已初始化。

**下一步**

- 补齐前端基础设施：`element-plus`、`pinia`、路由、基础布局。
- 实现文档上传/列表、知识库管理、生成任务、结果查看、用例审核编辑页面。
- 补齐错误提示、日志定位与端到端联调路径。

**验收标准**

- 不依赖手工 API 调用即可完成“上传资料 -> 生成用例 -> 审核提交”。
- 主流程页面可用，失败场景有明确提示和可定位日志。

## 风险与约束

- 知识库当前仍采用“整篇文档一个 embedding”，召回粒度弱于文档分块方案。
- `vector + ivfflat` 的高维约束已影响 embedding 模型选择。
- 历史异维度向量虽可治理，但重建速度、失败恢复、稳定性仍需持续观察。
- 多 Agent 协同当前更接近“骨架可跑”，距离稳定生产测试用例仍有差距。

## 参考文件

- 规格说明：`docs/spec.md`
- 配置示例：`backend/configs/config-example.yaml`
- 数据库迁移：`backend/migrations/001_init.sql`
- 路由定义：`backend/internal/api/router/router.go`
- 本地联调脚本：`dev.sh`

## 维护约定

- 所有进度更新只改“迭代详情”，不再新增独立的进度快照区。
- 每次更新“已完成”时补充日期或明确“截至日期”。
- 每个迭代必须同时维护“下一步”，避免计划和执行脱节。
