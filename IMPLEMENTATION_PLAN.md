# CaseAgent 实施计划（按能力域与迭代维护）

## 文档用途

这份文档从现在开始只做三件事：

1. 定义项目要交付的核心能力。
2. 记录当前代码和既有联调结论对应的真实状态。
3. 给出后续实现的实际执行顺序。

这里的“能力域”不再表示先后顺序，只表示功能边界；真正的排期和推进顺序，以“当前迭代计划”为准。

## 项目目标

CaseAgent 的目标是围绕“需求文档 + 架构知识”生成可审核的测试用例，形成以下闭环：

1. 导入需求文档与架构知识。
2. 对文档进行清洗、分块、向量化与检索。
3. 识别受影响产品和模块。
4. 基于需求与知识上下文生成测试用例。
5. 对生成结果进行审核、修改与提交。
6. 必要时沉淀新的知识库更新建议。

## 固定技术决策

- 前端：Vue 3 + Vite。当前仅有脚手架，后续页面按业务闭环逐步补齐。
- 后端：Golang + Gin + Bun ORM + eino + eino-ext。
- 数据库：PostgreSQL + pgvector。
- 模型配置统一走配置文件，配置文件加入 `.gitignore`，并支持环境变量覆盖。
- chat 当前支持 Ark；embedding 当前支持 Ark / OpenAI-compatible。
- embedding 维度通过 `model.embedding.dimensions` 显式配置。
- 当前工程上对 `Qwen3-Embedding-8B` 采用 `2000` 维输出，以兼容 `pgvector + ivfflat` 的索引约束。
- 如后续需要高于 `2000` 维的向量方案，需要重新评估向量类型与索引策略，不能直接沿用当前方案。
- Google Drive 集成通过本地 `gws drive files export` 命令调用，无 Docker 依赖。

## 当前实现快照

以下判断基于当前仓库代码结构和既有联调记录整理，不等同于“今天已全部重新回归验证”。

- 后端基础结构已落地：项目结构、配置加载、Gin 路由、Bun 模型、迁移脚本、服务层骨架都在。
- 数据层已具备 pgvector 自定义向量编码/解码，以及按当前 embedding 维度自动校正向量列的能力。
- 文档处理服务已支持：
  - 上传后异步处理；
  - Google Drive 文档导出；
  - base64 图片清洗；
  - 按 Markdown 标题分块；
  - embedding 落库到 `document_chunks`；
  - 保存原始文档内容；
  - 单文档重处理。
- 知识库服务已支持：
  - CRUD；
  - “整篇文档一个 embedding”的向量化写入；
  - 处理状态跟踪；
  - 单知识库重处理。
- 检索服务已具备文档检索和知识库检索的基础 API。
- 文档检索已支持 multi-query 合并去重，并按父文档聚合命中片段。
- 知识库检索已支持 multi-query 合并去重（按多查询召回结果融合排序）。
- 已有向量维护入口，可查看当前维度下的向量健康状态，并批量重建需要重处理的文档/知识库。
- 后端启动时会自动应用当前唯一 schema 文件 `backend/migrations/001_init.sql`，测试库不再依赖手工建表。
- 任务服务已具备：
  - 需求文档装载；
  - 受影响产品/模块初步识别；
  - 需求文档检索上下文拼装（multi-query 命中片段聚合）；
  - 任务审核/生成入口的状态流转校验（合法状态才能进入下一步）；
  - 调用 Agent 服务生成测试用例；
  - 用例按任务落库。
- DeepAgent 与 4 个子 Agent 的代码骨架已存在，但复杂协同、去重和调度仍未完成。
- 前端目前仍是 Vite + Vue 默认脚手架，业务页面尚未开始。
- 本地前后端联调脚本 `dev.sh` 已存在。

## 能力域状态矩阵

| 能力域 | 当前状态 | 已完成内容 | 主要缺口 |
| --- | --- | --- | --- |
| 基础设施与数据模型 | 进行中 | Go 项目结构、配置体系、Bun 模型、迁移脚本、基础路由、向量列维度校正 | 前端基础设施未补齐；错误处理、日志、测试覆盖还不完整 |
| 文档处理 | 进行中 | 文档上传、Google Drive 导入、清洗、分块、embedding、`document_chunks` 存储、状态更新、原文持久化、单文档重处理 | 文档链路的完整真实联调仍需补；parent retriever 闭环未完成 |
| 知识库 | 进行中 | CRUD、整篇文档 embedding、处理状态跟踪、单知识库重处理、基础检索链路 | 仍是“整篇文档一个向量”；与文档分块策略尚未形成统一检索体验 |
| 检索增强 | 进行中 | pgvector 检索、按当前维度过滤历史异维度向量、文档/知识库检索 API、文档/知识库 multi-query 合并检索、向量健康检查与批量重建入口 | parent retriever 粒度仍待继续细化，需求与知识上下文拼装策略仍待继续完善 |
| 用例生成与 Agent 协同 | 进行中 | 任务分析、受影响范围初步识别、任务状态流转基础校验、Agent 服务接入、生成结果落库、生成结果基础去重 | DeepAgent 协调逻辑、任务分发和质量控制还不够完整 |
| 审核与知识沉淀 | 进行中 | 测试用例查询、修改、提交相关 API 已有基础接口 | 知识库更新建议/确认流程未实现，审核链路尚缺前端承载 |
| 前端工作台 | 未开始 | Vue 3 + Vite 脚手架 | `element-plus`、`pinia`、路由、基础布局，以及所有业务页面均未开始 |

## 实施原则

后续实现统一遵循以下原则：

1. 先打通后端业务闭环，再补前端承载，不把前端脚手架当作后端推进的前置阻塞。
2. 每个迭代都必须有可以验证的闭环结果，避免只完成一组分散接口。
3. 技术决策变化时，更新“固定技术决策”或“风险与待办”，不要再把它写成新的阶段。
4. 进度记录以“当前状态 + 当前迭代”表达，不再用“阶段完成百分比”描述。

## 当前迭代计划

### 迭代 1：打通数据闭环（最高优先级）

**目标**

把“文档/知识库写入 -> 向量化 -> 检索”两条链路补到稳定可用，先解决后续所有生成能力依赖的数据基础。

**范围**

- 补齐文档上传链路的真实联调。
- 校验文档分块、embedding 写入、检索返回的一致性。
- 校验知识库写入、embedding、检索链路。
- 清理或重建历史异维度向量数据，消除 `1024` 维旧数据带来的长期隐患。
- 把当前检索结果的可验证性补强到足以支撑后续生成链路。

**验收标准**

- 文档链路可从上传走到检索命中。
- 知识库链路可从创建走到检索命中。
- 当前 `2000` 维配置下，不再因历史异维度向量导致检索报错或结果混乱。
- 至少有一条稳定的本地联调路径可重复执行。

### 迭代 2：打通生成闭环

**目标**

在数据闭环稳定后，完成“需求输入 -> 受影响范围确认 -> 检索增强 -> Agent 生成 -> 用例落库”的主业务闭环。

**范围**

- 完成 parent retriever。
- 完成 multi-query 检索。
- 完成需求与知识上下文的拼装策略。
- 完成 DeepAgent 的实际协调逻辑。
- 完成子 Agent 结果去重、汇总与结构化输出。
- 补齐任务状态流转和生成链路的稳定性。

**验收标准**

- 可基于选定需求文档生成一批结构正确的测试用例。
- 生成过程能明确看到受影响产品/模块与最终用例之间的上下文关系。
- 结果可稳定落库，并可被后续审核接口读取。

### 迭代 3：打通产品化闭环

**目标**

在主后端流程可用后，补齐前端工作台、审核体验和运维性，让系统不依赖手工 API 调用也能完整走通。

**范围**

- 前端基础设施：`element-plus`、`pinia`、路由、基础布局。
- 文档上传与列表页面。
- 知识库管理页面。
- 生成任务创建、受影响范围确认、进度展示、结果查看页面。
- 用例审核与编辑页面。
- 知识库更新建议/确认页面。
- 错误处理、日志、端到端联调补齐。

**验收标准**

- 不依赖手工调用 API，也能完成一轮“上传资料 -> 生成用例 -> 审核提交”。
- 页面能承载主流程，不再只是调试用入口。
- 主要失败场景有清晰提示和可定位日志。

## 当前优先待办

后续实现默认按以下顺序推进：

1. 补文档链路真实联调，确认上传、分块、向量化、检索闭环稳定。
2. 使用已有重处理与批量重建入口，完成历史异维度向量和缺向量数据治理。
3. 补强检索验证能力，为 parent retriever 和 multi-query 留出稳定基线。
4. 在数据闭环稳定后，进入 Agent 生成链路补齐。
5. 最后再系统性补前端工作台和审核体验。

## 风险与待办

- 当前知识库仍采用“整篇文档一个 embedding”，这在召回粒度上弱于文档分块方案，后续可能需要统一检索粒度。
- 当前 `vector + ivfflat` 对高维向量的约束已经影响模型选择，后续如切换 embedding 方案，需要先评估索引能力。
- 历史异维度向量虽然已可通过批量重建入口治理，但实际联调中仍需要确认重建速度、失败恢复和结果稳定性。
- 多 Agent 协同目前更接近“骨架可跑”，距离稳定的测试用例生产仍有明显差距。
- 前端尚未开始，后期需要控制页面补齐节奏，避免与后端闭环争抢优先级。

## 既有联调记录

以下是当前文档继续保留的、仍对后续实现有指导意义的联调结论；最近一次记录时间为 `2026-04-23`。

1. mixed provider 已恢复，`chat=ark`、`embedding=openai-compatible` 可以编译运行。
2. `KnowledgeBase` 表名映射问题已修复，当前正确访问 `knowledge_base`。
3. pgvector 列直接写 `[]float32` 的问题已修复，当前通过自定义 vector 类型编码写入。
4. `Qwen3-Embedding-8B` 通过 openai-compatible 网关接入时，当前工程选择显式使用 `2000` 维输出：
   - `4096` 维虽然更接近模型原生维度，但当前 `pgvector + ivfflat` 方案无法建立可用近似索引。
   - `2000` 维是当前技术栈下继续使用 `vector + ivfflat` 的可行上限方案。
5. 知识库链路已有过真实联调记录：
   - `POST /api/v1/knowledge` 可创建记录。
   - 异步 embedding 可成功更新为 `2000` 维向量。
   - `POST /api/v1/retrieval/knowledge` 可成功召回新写入的知识库。
6. 当前数据库中如混有历史 `1024` 维旧向量，检索层会按当前配置维度过滤，避免新旧向量混查时报错；但后续仍建议执行一次清理或重建。
7. `2026-04-28` 已验证后端可连接本地 PostgreSQL、自动应用 schema 并成功启动监听；本地 `curl` 在当前执行环境中未打通，更像是沙箱访问限制，不是服务未启动。

## 参考文件

- 规格说明：`docs/spec.md`
- 配置示例：`backend/configs/config-example.yaml`
- 数据库迁移：`backend/migrations/001_init.sql`
- 路由定义：`backend/internal/api/router/router.go`
- 本地联调脚本：`dev.sh`

## 维护约定

后续维护这份文档时，统一按下面的方式更新：

1. 代码能力新增或完成时，更新“能力域状态矩阵”。
2. 推进顺序变化时，更新“当前迭代计划”。
3. 新的技术约束或方案收敛时，更新“固定技术决策”或“风险与待办”。
4. 真实跑通过的联调结果，更新到“既有联调记录”，并注明日期。
