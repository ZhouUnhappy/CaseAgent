# I1 模块知识库：控制平面

## 模块职责

控制平面负责调度升级任务、检查节点健康状态和协调回滚流程。

## 健康探针

控制平面健康探针端口为 `18080`，探针名称为 `control-plane-probe`。升级前必须连续通过两次健康探针检查。

## 回滚流程

当健康探针失败时，控制平面会暂停升级任务并保留 30 分钟回滚窗口。回滚成功后，模块会写入 `probe-gate-7781 rollback completed` 审计事件。

## E2E 测试关注点

- 健康探针失败后任务进入暂停状态。
- 回滚窗口内允许触发回滚。
- 回滚完成后生成可检索审计事件。
